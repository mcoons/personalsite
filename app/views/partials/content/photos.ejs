  <div class="pixgrid clearfix">
    <% if (photos.length > 0) { %>
    <% for (i=0; i<photos.length; i++) { %>
        <img src="/images/photos/<%= photos[i] %>" alt="Photo <%= i %>">
    <% } %>
    <% } %>
  </div>
